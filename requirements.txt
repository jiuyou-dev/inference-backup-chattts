# ============================================================
# ChatTTS 完整依赖 (推理模式)
# ============================================================
# 核心框架
torch>=2.1.0
torchaudio
torchvision

# 数值计算
numpy>=1.22.0,<3.0.0
scipy>=1.7.0

# 音频处理
soundfile>=0.11.0
pydub>=0.25.0
av>=10.0.0

# ChatTTS 特定
vector-quantize-pytorch
transformers>=4.41.1
vocos
pybase16384

# 加速
numba>=0.56.0

# 网络/下载
requests>=2.28.0

# 进度条
tqdm

# 备注:
# - fairseq 在 Windows 上需要编译，可能会失败
# - RTX 5060 Ti (sm_120) 需要较新的 PyTorch 版本才能支持 CUDA
#   如需 GPU 加速，请使用 torch>=2.6.0 或从源码编译
